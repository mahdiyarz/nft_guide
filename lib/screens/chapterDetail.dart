import 'package:flutter/material.dart';

class ChapterDetail extends StatelessWidget {
  const ChapterDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('موضوع فصل'),
      ),
      body: Container(),
    );
  }
}
